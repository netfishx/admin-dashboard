build:
  stage: build
  only:
    - test
  script:
    - docker build -t admin-006:$CI_COMMIT_SHORT_SHA .
    - docker stop admin-006 || true
    - docker rm admin-006 || true
    - docker run -d -p 3333:3000 --name admin-006 --network demo --log-opt max-size=100m admin-006:$CI_COMMIT_SHORT_SHA
